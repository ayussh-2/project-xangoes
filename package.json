{
    "name": "project-xangoes",
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "build": "turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "test": "turbo test",
        "run-api": "turbo dev --filter=api",
        "db:studio": "turbo db:studio --filter=api",
        "db:generate": "turbo db:generate --filter=api"
    },
    "devDependencies": {
        "turbo": "^2.0.0"
    },
    "packageManager": "bun@1.0.0"
}
